/* 
 * I was automatically generated by pn_cache_RV32I_buildscript.py,
 * invoked by libparanut Makefile -> No edits!
 */

/** @file */

/**
 * \internal
 * \file
 * \brief        Contains RV32I assembly implementations of assembly
 *               functions called in \ref pn_cache.c for cache line
 *               size 256.
 *
 * This file (for this specific architecture, others may do it
 * differently) was created by \ref pn_cache_RV32I_buildscript.py
 * while compiling (automatically called by \ref Makefile), as
 * are the other files with the pattern pn_cache_RV32I_*.S for name.
 * The reasons for this are described in the \ref
 * pn_cache_RV32I_buildscript.py documentation itself.
 *
 * The code shown below was created with a cache line size of 256 Bit.
 *
 * So far, the following cache line sizes are available (in bits):
 * 32 64 128 256 512 1024 2048
 *
 * Contains implementations of:
 *    - \ref enable_cache_as()
 *    - \ref disable_cache_as()
 *    - \ref cache_banks_as()
 *    - \ref cache_sets_as()
 *    - \ref mem_size_as()
 *    - \ref invalidate_256_as()
 *    - \ref invalidate_bulk_256_as()
 *    - \ref writeback_256_as()
 *    - \ref writeback_bulk_256_as()
 *    - \ref flush_256_as()
 *    - \ref flush_bulk_256_as()
 *
 * \includelineno pn_cache/pn_cache_RV32I_256.S
 */

/*
 * Put in here so Doxygen will know that it is implemented in this
 * file. Sadly, Doxygen has no built in assembly interpreter, so we
 * are stuck with this.
 */

#ifdef DOXYGEN

  /**
    * \addtogroup as
    * @{
    */

  /**
    * @{
    */

  /**
   * \internal
   * \fn         void enable_cache_as(void)
   * \brief      Enables cache.
   */
   void enable_cache_as(void) {}

  /**
   * \internal
   * \fn         void disable_cache_as(void)
   * \brief      Disables cache.
   */
   void disable_cache_as(void) {}

  /**
   * \internal
   * \fn         unsigned int cache_banks_as(void)
   * \brief      Returns number of cache banks.
   *
   * \return     Number of cache banks.
   */
   unsigned int cache_banks_as(void) {}

  /**
   * \internal
   * \fn         unsigned int cache_sets_as(void)
   * \brief      Returns number of cache sets.
   *
   * \return     Number of cache sets.
   */
   unsigned int cache_sets_as(void) {}

  /**
   * \internal
   * \fn         unsigned int mem_size_as(void)
   * \brief      Reads memory size.
   *
   * \return     Content of pnmemsize register.
   */
   unsigned int mem_size_as(void) {}

  /**
   * \internal
   * \fn         void invalidate_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Invalidates single cache lines until end address
   *             is reached.
   *
   * Will at least invalidate one line.
   */
   void invalidate_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
   * \internal
   * \fn         void invalidate_bulk_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Invalidates 32 cache lines at once until end 
   *             address is reached.
   *
   * Will at least invalidate 32 lines.
   */
   void invalidate_bulk_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
   * \internal
   * \fn         void writeback_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Writes back single cache lines until end address
   *             is reached.
   *
   * Will at least writeback one line.
   */
   void writeback_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
   * \internal
   * \fn         void writeback_bulk_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Writes back 32 cache lines at once until end 
   *             address is reached.
   *
   * Will at least writeback 32 lines.
   */
   void writeback_bulk_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
   * \internal
   * \fn         void flush_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Flushes single cache lines until end address
   *             is reached.
   *
   * Will at least flush one line.
   */
   void flush_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
   * \internal
   * \fn         void flush_bulk_256_as(unsigned int addr, unsigned int endaddr)
   * \brief      Flushes 32 cache lines at once until end 
   *             address is reached.
   *
   * Will at least flush 32 lines.
   */
   void flush_bulk_256_as(unsigned int addr, unsigned int endaddr) {}

  /**
    * @}
    */

  /**
    * @}
    */

#endif /* DOXYGEN */

/*Header***********************************************************************/

#ifndef DOXYGEN

.text                                  /* enter text section                  */
.align 2                               /* align Code to 2^2 Bytes             */

/* declare labels in here to be global */
.globl enable_cache_as
.globl disable_cache_as
.globl cache_banks_as
.globl cache_sets_as
.globl mem_size_as
.globl invalidate_256_as
.globl invalidate_bulk_256_as
.globl writeback_256_as
.globl writeback_bulk_256_as
.globl flush_256_as
.globl flush_bulk_256_as

/* ParaNut Custom Registers and Instructions */
#include "custom_RV32I.S"

/*Functions********************************************************************/

enable_cache_as:
   fence
   li   t0,      3
   csrs pncache, t0
   ret

/*----------------------------------------------------------------------------*/

disable_cache_as:
   fence
   li   t0,      0
   csrw pncache, t0
   ret

/*----------------------------------------------------------------------------*/

cache_banks_as:
   csrr a0,      pncacheinfo
   srli a0,      a0,      8
   ret

/*----------------------------------------------------------------------------*/

cache_sets_as:
  csrr a0,       pncachesets
  ret

/*----------------------------------------------------------------------------*/

mem_size_as:
  csrr a0,       pnmemsize
  ret

/*----------------------------------------------------------------------------*/

invalidate_256_as:
   fence
invalidate_256_as_loop:
   CINV(10, 0x000)
   addi a0, a0, 32
   blt a0, a1, invalidate_256_as_loop
   ret

/*----------------------------------------------------------------------------*/

invalidate_bulk_256_as:
   fence
invalidate_bulk_256_as_loop:
   CINV(10, 0)
   CINV(10, 32)
   CINV(10, 64)
   CINV(10, 96)
   CINV(10, 128)
   CINV(10, 160)
   CINV(10, 192)
   CINV(10, 224)
   CINV(10, 256)
   CINV(10, 288)
   CINV(10, 320)
   CINV(10, 352)
   CINV(10, 384)
   CINV(10, 416)
   CINV(10, 448)
   CINV(10, 480)
   CINV(10, 512)
   CINV(10, 544)
   CINV(10, 576)
   CINV(10, 608)
   CINV(10, 640)
   CINV(10, 672)
   CINV(10, 704)
   CINV(10, 736)
   CINV(10, 768)
   CINV(10, 800)
   CINV(10, 832)
   CINV(10, 864)
   CINV(10, 896)
   CINV(10, 928)
   CINV(10, 960)
   CINV(10, 992)
   addi a0, a0, 1024
   blt  a0, a1, invalidate_bulk_256_as_loop 
   ret

/*----------------------------------------------------------------------------*/

writeback_256_as:
   fence
writeback_256_as_loop:
   CWB(10, 0x000)
   addi a0, a0, 32
   blt a0, a1, writeback_256_as_loop
   ret

/*----------------------------------------------------------------------------*/

writeback_bulk_256_as:
   fence
writeback_bulk_256_as_loop:
   CWB(10, 0)
   CWB(10, 32)
   CWB(10, 64)
   CWB(10, 96)
   CWB(10, 128)
   CWB(10, 160)
   CWB(10, 192)
   CWB(10, 224)
   CWB(10, 256)
   CWB(10, 288)
   CWB(10, 320)
   CWB(10, 352)
   CWB(10, 384)
   CWB(10, 416)
   CWB(10, 448)
   CWB(10, 480)
   CWB(10, 512)
   CWB(10, 544)
   CWB(10, 576)
   CWB(10, 608)
   CWB(10, 640)
   CWB(10, 672)
   CWB(10, 704)
   CWB(10, 736)
   CWB(10, 768)
   CWB(10, 800)
   CWB(10, 832)
   CWB(10, 864)
   CWB(10, 896)
   CWB(10, 928)
   CWB(10, 960)
   CWB(10, 992)
   addi a0, a0, 1024
   blt  a0, a1, writeback_bulk_256_as_loop 
   ret

/*----------------------------------------------------------------------------*/

flush_256_as:
   fence
flush_256_as_loop:
   CFLUSH(10, 0x000)
   addi a0, a0, 32
   blt a0, a1, flush_256_as_loop
   ret

/*----------------------------------------------------------------------------*/

flush_bulk_256_as:
   fence
flush_bulk_256_as_loop:
   CFLUSH(10, 0)
   CFLUSH(10, 32)
   CFLUSH(10, 64)
   CFLUSH(10, 96)
   CFLUSH(10, 128)
   CFLUSH(10, 160)
   CFLUSH(10, 192)
   CFLUSH(10, 224)
   CFLUSH(10, 256)
   CFLUSH(10, 288)
   CFLUSH(10, 320)
   CFLUSH(10, 352)
   CFLUSH(10, 384)
   CFLUSH(10, 416)
   CFLUSH(10, 448)
   CFLUSH(10, 480)
   CFLUSH(10, 512)
   CFLUSH(10, 544)
   CFLUSH(10, 576)
   CFLUSH(10, 608)
   CFLUSH(10, 640)
   CFLUSH(10, 672)
   CFLUSH(10, 704)
   CFLUSH(10, 736)
   CFLUSH(10, 768)
   CFLUSH(10, 800)
   CFLUSH(10, 832)
   CFLUSH(10, 864)
   CFLUSH(10, 896)
   CFLUSH(10, 928)
   CFLUSH(10, 960)
   CFLUSH(10, 992)
   addi a0, a0, 1024
   blt  a0, a1, flush_bulk_256_as_loop 
   ret

#endif /* !DOXYGEN */

/*EOF**************************************************************************/
